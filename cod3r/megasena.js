// let mega = ['', '', '', '','',''];
// let numero_aleatorio;
// for (let i =  0; i < mega.length; i++) {
//     numero_aleatorio = (Math.random() * 60 + 1).toFixed(2)
//     numero_fix = Math.round(numero_aleatorio)
//     mega[i] = numero_fix
//     console.log(mega[i])
//     }

    let mega = ['', '', '', '','',''];
    for (nums in mega) {
        
        if (nums == mega.length && nums == nums) {
            continue
     
        }
        nums = Math.random() * (59+ 1).toFixed(1)
        console.log(`O numero escolhido foi: ${Math.round(nums)}`)
    }

    let mega = ['', '', '', '','',''];
    for (nums in mega) {
        
        if (nums == mega.length && nums == nums) {
            continue
     
        }
        nums = Math.random() * (59+ 1).toFixed(1)
        console.log(`O numero escolhido foi: ${Math.round(nums)}`)
    }